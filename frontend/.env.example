# Frontend environment variables. Copy to .env or .env.development.
# Vite embeds these at build time (VITE_* only).

# Backend API URL (required for API usage)
VITE_API_URL=http://localhost:3000

# Google OAuth (required for Google login)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

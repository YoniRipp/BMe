/** Approximate cost per Gemini voice API call (USD). */
export const VOICE_COST_PER_CALL = 0.005;

/** Monthly voice call threshold to flag a user as heavy. */
export const VOICE_HEAVY_THRESHOLD = 50;

---
description: Run TypeScript checks on both backend and frontend
allowed-tools: Bash
---

Run full typecheck on the project:

!`cd backend && npx tsc --noEmit`
!`cd frontend && npx tsc --noEmit`

Report any errors and suggest fixes.
